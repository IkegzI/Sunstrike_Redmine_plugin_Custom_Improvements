<% @settings = ActionController::Parameters.new(@settings) unless Rails.version >= '5.2' %>

<%= render_tabs { name: 'overrun', partial: 'improvements/settings/improvements', label: :label_settings_menu } %>